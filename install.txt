pip uninstall dm-env
pip uninstall dm-acme
pip uninstall dm-acme[reverb]
pip uninstall dm-sonnet
pip uninstall trfl
pip uninstall dm-reverb
pip uninstall dm-reverb[tensorflow]
pip uninstall dm-acme[launchpad]

pip install dm-env
pip install dm-acme
pip install dm-acme[reverb]
pip install dm-sonnet
pip install trfl
pip install dm-reverb
pip install dm-reverb[tensorflow]
pip install dm-acme[launchpad]



# install acme and reverb properly to fix errorlog.txt
